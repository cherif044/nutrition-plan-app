PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nutrition_plan
DB_USER=cherif
DB_PASSWORD=your_db_password_here

# JWT — generate a secure random secret, e.g.:  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_a_long_random_secret

NODE_ENV=development
