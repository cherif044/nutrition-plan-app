# Fix prompt — feasibility pre-check in selectTemplateForMeal (do NOT touch scoring/retry loop)

Stop — do not modify the retry loop, mealScore, or how selectInitialItems competes with template results. That comparison is correct and must stay.

The actual problem: selectTemplateForMeal picks a template without checking if its components can realistically reach the meal's target macros within their maxServingG/minServingG bounds. Fix this by adding a feasibility check inside selectTemplateForMeal itself, before returning a template.

## Fix

In `selectTemplateForMeal`, after filtering templates by mealType/allowedFoods/dietTags (existing logic, keep as-is), add one more filter step before picking:

For each surviving template, check feasibility against the meal's target:
1. Build the candidate item list (same mapping you already do: each component's foodId → food, quantityG: food.defaultServingG).
2. Call the existing `solvePortionsLeastSquares(items, { proteinG: target.proteinG, carbG: target.carbG, fatG: target.fatG })` (already in this file) to get the best achievable result for this template's foods.
3. Check if the result is within tolerance using the same logic `checkRebalanceFeasibility` already uses (NUTRITION.proteinToleranceG / carbToleranceG / fatToleranceG against the solved totals).
4. Keep only templates that pass this feasibility check. Discard the rest.

If zero templates remain after this feasibility filter, return `null` (existing fallback to selectInitialItems triggers, unchanged).

If one or more templates remain, pick one at random from the feasible list (same `pick()` helper, same seed approach as before).

## Constraints
- Do not change mealScore, the attempt/retry loop in generateMeal, or anything in generatePlanFreeform.
- Do not change mealTemplates.json or foods.json.
- Reuse solvePortionsLeastSquares and the tolerance constants already in this file — do not write new solving logic.

## Report back (required before we proceed to anything else)
Run generatePlan once for a standard-diet user. Report:
1. How many of the 341 breakfast templates now pass the feasibility check for a ~734 kcal breakfast target (just the count).
2. Whether a template was actually selected and used in the final output for at least one meal, or whether it still fell back to selectInitialItems — and if it fell back, confirm that's because zero templates were feasible (not a bug).
3. Confirm no changes were made outside selectTemplateForMeal.